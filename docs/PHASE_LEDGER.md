# Phase Accountability Ledger

状态只使用：`implemented`、`evaluated-local`、`not applicable`、`blocked`、`accepted omission`。

| Phase / artifact | Scope | Status | Decision / reason | Evidence | Verification | Remaining risk |
|---|---|---|---|---|---|---|
| Discovery response | greenfield full V1 | implemented | 用户授权代理决定 F-design 选择 | 当前目标措辞；`DESIGN.md` | design record reviewed locally | 实施中可能出现新兼容分支 |
| Delivery mode | all UI | implemented | delegated direct implementation | `DESIGN.md` | no generated references created | none |
| Reference intake | all UI | implemented | delegated no-reference | `DESIGN.md` | product plan used as project truth | native visual QA pending |
| Architecture decision | full app | implemented | plan-defined native Swift architecture | plan + `AGENTS.md` + `DESIGN.md` | external Xcode 27.0 SDK/signature/first-launch verified | beta toolchain drift |
| Product brief | full app | implemented | developer/operations tool | `DESIGN.md` | local review against V2.0 plan | outcome tests pending |
| Surface strategy | all material surfaces | implemented | per-surface task topology | `DESIGN.md` | structural convergence review | runtime data ranges pending |
| Structural exploration | greenfield | implemented | status-first shell with task workspace and connection flow overrides | `DESIGN.md` matrix | bounded-originality review | native screenshot QA pending |
| Page/state map | daily-operation and first-run surfaces | implemented | eight daily routes plus a stable nine-step Onboarding flow use explicit loading/empty/failed/ready or pending/checking/warning/blocked states | `DESIGN.md`; `BridgePresentation`; `BridgeAppShell` | Presentation and AppShell state/semantics XCTest | rendered compact/light-dark/VoiceOver acceptance pending |
| Design system/color/state | daily-operation UI | evaluated-local | native semantic system, SF Symbols, text+icon status and Reduced Motion; no parallel component library | `DESIGN.md`; `BridgePresentation` | strict build/format, Presentation semantics XCTest and a real first-run App accessibility audit | light/dark, VoiceOver and reduced-motion capture evidence pending |
| Component decisions | initial UI primitives | evaluated-local | native SwiftUI/AppKit selected | `DESIGN.md` register | package/runtime check pending | complex Diff may need reevaluation |
| Codex framing/initialize/models | phase 0 | implemented | isolated Swift process + LF JSON transport + dynamic catalog | `Prototypes/AppServerProbe`; versioned schema | build, 4 probe XCTest, self-test, real initialize and model/list | future Codex drift |
| Production Codex RPC foundation | phases 0/4 | implemented | one-shot process session; bounded LF transport; handshake isolation; typed initialize/models/thread list/resume; lossless approval envelopes; strongly typed bounded command/file/permissions approval requests and item evidence; confirmed process reaping | `Packages/BridgeCore/Sources/BridgeCodexRPC` | 35 fake process/wire/lifecycle/approval-decoder XCTest | future Codex drift and real denial-semantics compatibility fixtures |
| Codex Thread/turn/steer/interrupt/Supervisor | phase 0 | implemented | stable typed wire plus isolated ephemeral read-only fixtures | `BridgeCodexRPC`; `CodexRPCFixture`; compatibility record | fake wire tests and real basic/steer/interrupt/Luna structured-output turns | future Codex drift |
| Domain reducer foundation | phases 1/4/5 | implemented | phase/activity separation, terminal/report invariants, approvals, stop intent and recovery | `BridgeDomain` | 17 reducer XCTest | public event wire schema remains internal/versioned |
| Persistence foundation | phases 1/4/7 | implemented | GRDB CAS event append, atomic claim plus initial projection, durable idempotency, dual locks and owner-only release; future event changes, terminal notification reservations and lifecycle preferences use durable SQLite cursors/ledgers without replaying historical rows at startup; notification preference boundaries advance the consumer cursor in the same transaction and history pruning is bounded; every internal migrator publishes a component-scoped `0600` SQLite online backup before applying a known pending migration and aborts before mutation if the private backup boundary fails; a separate versioned backup-package core now validates only caller-provided consistent snapshots of the three durable SQLite files with an allowlist, manifest, size/digest and private-file checks, while excluding supervisor HOME, Keychain and transient capability state | `BridgePersistence`; `BridgeAppShell/DesktopBackupPackage`; `BridgeAppShell/DesktopBackupRestore` | 31 SQLite behavior XCTest plus 7 real backup-package filesystem tests and 9 restore-coordinator tests covering atomic publish, source/package symlinks, extra files, digest tampering, manifest version, permission failures, crash rollback, data-root inode drift and lock-holding-task gates | only restart-time swap is supported (no runtime database replacement); credentialed live acceptance and signed release gates remain external |
| Secure project read boundary | phases 1/5 | implemented | component-aware containment, sensitive policy, descriptor-relative no-follow open, identity recheck and fail-closed outbound path/secret redaction | `BridgeSecurity` | 15 path/symlink/replacement/binary/limit/outbound-redaction XCTest | search layer and secure write adapter pending |
| Project registry foundation | phases 1/5 | implemented | opaque IDs, exact canonical root/worktree identity, durable three-state policy updates, forward-compatible permission decoding and path-free MCP summaries; confirmed native removal never deletes local files, while an offline single-root project can be reconnected only by explicitly selecting the same canonical path, atomically replacing its root identity and clearing stale Thread bindings; a project-scoped mutation gate excludes new submissions/resumes and both mutations reject active tasks | `BridgeProjects`; `BridgeRepositories`; `BridgeCoordinator`; Onboarding directory picker; Projects native policy editor | registry/root/worktree/DTO/decoding XCTest, exact-path/mismatched-path rebind tests, project mutation-gate contention tests and AppShell restart coverage for policy edits/removal/reconnect | multi-root/worktree volume-identity remediation pending |
| Deterministic policy foundation | phase 5 | implemented | project permissions are hard upper bounds; fixed executable identity, exact argv, forbidden-path glob and scoped limits | `BridgePolicy` | 8 command/file policy XCTest | authoritative approval correlation, secure write adapter and persistence pending |
| Project-bound Codex execution | phases 1/4 | implemented | only Registry-issued exact roots can start read-only Threads; response cwd is rechecked before binding | `BridgeExecution` | 3 state/binding XCTest | durable binding repository and workspace-write flow pending |
| Task coordination foundation | phases 4/6 | implemented | event-sourced admission, stable idempotent replay, persisted runtime intent, approval barrier, steer, dual-lock ownership, atomic terminal event/lock release, queued suspend/resume generation, finalization authorization and generation/sequence-bound read-only recovery; exact terminal facts reduce without a duplicate Turn while unattached in-progress observations remain unknown with locks until an explicit idempotent local suspension commits both recovery facts and lock release | `BridgeCoordinator`; `BridgeRuntime`; versioned Codex schema | 32 coordinator XCTest with real SQLite plus isolated reconciliation process tests | credentialed existing-Thread recovery acceptance pending |
| Git evidence foundation | phases 4/6 | implemented | fixed `/usr/bin/git`, descriptor-bound cwd, minimal environment, unsafe repo config/attributes rejection, stable double capture and durable bounded UTF-8-safe paged patch storage; native terminal evidence is loaded on demand and bound to task/project/thread/turn/generation/sequence | `BridgeGit`; `BridgePipeline`; `BridgeAppShell` artifact projection | real-process/repository XCTest including root replacement, filter/fsmonitor non-execution, dirty attribution, bounded persistence, paging and typed terminal projection | checkpoint review/action still needs production Supervisor capability |
| Supervisor guard/runtime | phases 4/6 | implemented | semantic checkpoints and runtime reviews, strict structured output, outbound secret/path gate, contradictory-decision rejection, bounded anti-loop reducer, macOS Seatbelt evidence-only profile and per-task isolated user-selected Supervisor/read-only/no-network app-server; Luna is the default recommendation, not a built-in model; durable steer/suspend/interrupt actions bind task/project/thread/turn/generation/event sequence, use Coordinator CAS, and remain ambiguous after uncertain RPCs; production final review gates completion | `BridgeSupervisor`; `BridgePipeline`; `TaskCoordinator`; `docs/CODEX_PROTOCOL_COMPATIBILITY.md` | checkpoint/schema/reducer/egress/runtime/orchestrator XCTest, action-applied side-effect test, ledger v1-to-v2 migration/restart/corruption checks, exact-model failure, and malicious project/user read-write rejection | isolated HOME authentication/configuration and production runtime enablement pending; ambiguous-action operator resolution and credentialed live end-to-end acceptance pending |
| Structured reporting | phases 4/6 | implemented | final facts require app-server, Git, verification, policy and Supervisor/user authority; untrusted narrative excluded; reports are immutable and scope-bound; App support bundles contain only bounded typed redacted facts and are atomically exported as private JSON | `BridgeReporting`; `BridgeRepositories`; `BridgePipeline`; `BridgeAppShell` | report/redaction/repository/finalizer/support-bundle and real filesystem export XCTest | credentialed end-to-end report acceptance pending |
| Local MCP | phases 2/4 | implemented | strict per-session SDK servers behind a hardened IPv4 loopback listener; secret-path development mode and stripped static-header Tunnel mode; five compatible read tools plus task/project tools; AppShell injects the durable application backend and enforces local confirmation before remote execution; no remote approval | `BridgeMCP`; `BridgeAppShell`; `docs/MCP_SWIFT_SDK_INTEGRATION.md` | MCP contract/admission/security XCTest, AppShell real-loopback submit-confirm-reject XCTest and Inspector 2.1.0 | credentialed ChatGPT/Tunnel acceptance and controlled slow-reader pressure pending |
| Connection transport composition | phases 3/7 | implemented | one AppShell actor owns local-only, authenticated Manual HTTPS and Secure Tunnel modes; replacement is serialized, explicit stop and failed wake revalidation leave remote admission closed, sleep closes remote admission synchronously and drains accepted submissions, and wake reopens only after active-task reconciliation, durable refresh and strict current-transport revalidation; an unexpectedly failed Secure helper is rebuilt with bounded `1s/2s/4s` backoff while local MCP stays live, but authentication diagnostics or three failed attempts require local intervention | `BridgeAppShell`; Onboarding and Connections projections; `docs/CHATGPT_DEVELOPER_MODE.md` | 24 transport lifecycle/gate/restart/contract XCTest, Onboarding behavior XCTest, bounded real-loopback client coverage and native App build | real external HTTPS, Runtime Key and ChatGPT Developer Mode acceptance require user-controlled endpoints and credentials |
| Desktop lifecycle and notifications | phase 7 | implemented | one private-data-root instance lease owns lifecycle consumption; EventStore's durable global change cursor and notification ledger bind terminal notifications to task/event sequence/kind; notifications are permission-gated and restart-safe, and their versioned safe route opens the exact task even when the initial UI snapshot is still loading; authoritative active snapshots drive idle-sleep prevention; startup/wake performs exact Thread/Turn reconciliation before reopening remote admission; shutdown stops MCP before draining local services | `BridgeAppShell`; `BridgePersistence`; `BridgeCoordinator`; native Settings projection | lifecycle service/coordinator, notification-route/pending-selection, instance lease, exact recovery and persistence restart/concurrency XCTest; full package and unsigned App build | real existing-Thread recovery plus end-user macOS notification permission/banner/click acceptance pending |
| Secure MCP Tunnel | phase 3 | implemented | pinned official v0.0.11 Universal 2 supply stage; Keychain SecretStore boundary; same-Team suspended-process identity gate; fd3 Runtime Key/fd4 MCP header; inode-bound private runtime; peer-PID UDS health; strict ready plus fresh poll; Prometheus sample values are parsed separately from optional scrape timestamps; AppShell restores and monitors the configured transport and replaces an unexpectedly exited helper with bounded retries without interrupting local MCP | `BridgeTunnel`; `BridgeAppShell`; `docs/TUNNEL_CLIENT_INTEGRATION.md`; supply/config scripts | real fake-helper lifecycle/exit XCTest, stale metric value plus fresh scrape timestamp admission regression, AppShell restart/exhaustion/shutdown XCTest, macOS dynamic SecCode test, archive build/hash verification and official helper `doctor` against the header-authenticated MCP fixture | real restricted Runtime Key, full official `/metrics` connected path, ChatGPT Developer Mode/reconnect acceptance and signed helper bundling pending |
| Durable application repositories | phases 1/4 | implemented | project config, security-policy edits, safe project unregister, exact-root Thread binding and immutable final reports share the GRDB database and survive restart; removal deletes bindings, roots and configuration in one transaction while leaving local files intact; concurrent uniqueness and tamper checks fail closed; repository schema upgrades share the component-scoped consistent migration-backup boundary | `BridgeRepositories`; `BridgePersistence` | 10 repository SQLite/restart/cross-connection/removal/tamper XCTest plus real migration-backup behavior tests | retention and backup restore UI pending |
| Live Execution runtime | phase 4 | implemented | each task owns one isolated app-server and event consumer; exact model/effort/root; new/resumed Thread generation; immutable bounded item/request-correlated approval evidence with persistence barrier and memory budget; file changes retain a complete 100-entry/2-MiB manifest, per-diff digest and root identity without raw diff; manifest/aggregate encoding is capped at 128/256 KiB; bounded observations; steer/interrupt; abort-before-unlock lifecycle; start and wake revalidate root identity and read-only recovery never starts a replacement Turn | `BridgeRuntime`; `BridgeCoordinator` | real fake-process lifecycle/approval/resume/steer/interrupt/reconciliation/race XCTest | all approvals remain deny-only because command argv is non-authoritative, permissions are turn-scoped, and current file mutation is not atomically fd-bound |
| Application query/tool adapter | phases 2/4/5 | implemented | project/thread/model/task/report and restricted file operations are composed behind MCP-safe DTOs; project roots and suspected credentials are not returned; Codex deep links require exact project-bound Thread cwd; local UI submissions use a distinct durable origin and enforce read-only/no-network with the exact catalog-selected execution and Supervisor model/effort (Luna is the default recommendation) | `BridgeApplication`; `BridgeFiles`; `BridgeMCP`; `BridgeAppShell` | application, isolated catalog deadline, local idempotency/policy, real filesystem, project/task-tool and AppShell loopback XCTest | richer native task evidence workspace adapter pending |
| Local verification runner | phase 5 | implemented | only pre-registered commands selected by stable digest can run after an exact task/project/root/generation-bound, expiring, one-time local authorization is atomically consumed; fixed cwd fd, bounded process lifecycle and structural hashed result | `BridgeVerification`; `BridgeAppShell` verification broker | real process/policy/timeout/reap and multi-instance authorization XCTest | not an OS sandbox; native authorization workflow acceptance pending |
| Execution/Supervisor composition | phases 4/6 | partially implemented | Execution persists bounded plan, completed-command and completed-file semantic facts before lifecycle delivery; the durable ledger now records scope-bound progress checkpoints, performs runtime reviews, and executes only bound pending actions through Coordinator CAS before marking them applied. Production Supervisor review and Supervisor-dependent submission remain fail-closed unavailable until the Seatbelt boundary also has an isolated authenticated Codex configuration | `BridgeRuntime`; `BridgeCoordinator`; `BridgePipeline`; `BridgeSupervisor`; `BridgeAppShell`; `docs/CODEX_PROTOCOL_COMPATIBILITY.md` | semantic wire/runtime/persistence/checkpoint/review/action XCTest, durable ledger restart/concurrency/corruption/capacity/migration XCTest, malicious boundary fixture, production no-process-start rejection, full package/Inspector/App build | isolated HOME authentication/configuration, finalization recovery and credentialed live end-to-end acceptance |
| Native UI presentation | phases 1/7 | evaluated-local | runnable AppShell, shared main-window/menu-bar runtime, eight daily pages, nine-step Onboarding, three-mode connection setup, project registration/policy/removal/offline reconnect, project-scoped Thread/history browser, dynamic-model local read-only composer, user-triggered typed Codex account-limit projection, on-demand typed terminal evidence workspace, explicit ambiguous-recovery suspension, ambiguous Supervisor action review/confirmation, typed support-bundle export, notification/idle/login-item settings and fail-closed decision adapters are implemented in native SwiftUI/AppKit; menu bar exposes active/pending counts and a persistent pause/resume control for new remote submissions, while task/project/thread workspaces use compact navigation fallback | `App`; `CodexBridge.xcodeproj`; `BridgeAppShell`; `BridgePresentation`; `BridgeAppModel`; `UITests` | AppShell XCTest, Presentation/AppModel XCTest, strict format, unsigned arm64 XCUITest covering first-run window geometry, nine-step semantics, hit regions, element descriptions, named status item and menu commands; persisted receiving pause and admission-drain XCTest | continuous checkpoint projection, remaining-page light/dark/VoiceOver/reduced-motion UI matrix and signed Universal 2 acceptance pending |
| Motion coverage | native UI | evaluated-local | only restrained navigation/state continuity is permitted and Reduced Motion removes movement | `DESIGN.md`; `BridgePresentation` | semantic/unit coverage | normal/reduced real App capture pending |
| Media transitions | no media-led surfaces | not applicable | V1 is evidence-led native tool | `DESIGN.md` | contract review | none |
| Browser/mobile QA | native macOS only | not applicable | replaced by native window/a11y matrix | V2.0 plan + `DESIGN.md` | first-run native XCUITest and accessibility audit passed on Xcode 27 Beta 5 | broader native page/state matrix pending |
| Packaging/release | phase 7 | evaluated-local | the native App has a complete icon set and reproducible unsigned Universal 2 archive path; the verified pinned helper is staged before the outer App, with post-stage digest and notices; ZIP, DMG, SPDX SBOM and checksums are generated as explicitly unsigned candidates while credentialed release signing remains separate | `App`; `CodexBridge.xcodeproj`; `Scripts/build-release-candidate.sh`; `Scripts/stage-tunnel-helper.sh`; `docs/RELEASE.md`; root legal/privacy files | Xcode 27 Beta 5 arm64 Debug build plus unsigned arm64/x86_64 archive, helper staging, bundle and package inspection | Developer ID signing, notarization, staple, credentialed ChatGPT/Tunnel and clean-Mac Gatekeeper acceptance pending |
| Open-source continuous integration | phase 7 | implemented | pull requests, `main` pushes and manual runs use a credential-free ARM64 Xcode 27 workflow with read-only repository permissions; package tests, strict formatting, Inspector contract, unsigned App build and isolated native accessibility smoke test are separate gates | `.github/workflows/ci.yml`; `CONTRIBUTING.md` | workflow YAML parsed locally; every mirrored command passed with Xcode 27 Beta 5, including the full Swift package, Inspector 2.1.0, unsigned App build and XCUITest | first hosted `xcode-27` run remains pending until the commit is pushed; public-preview runner drift does not replace signed release acceptance |

Update this ledger whenever code or runtime evidence changes a status. `blocked` here describes an artifact state, not the overall goal status.

## Latest Local Evidence (2026-08-16)

- Commit `ff390bf` adds a credential-free `universal2` CI job. The matching local Xcode 27 Beta 5 archive verified both `arm64` and `x86_64` slices.
- `Scripts/build-release-candidate.sh` produced an unsigned Universal 2 ZIP/DMG candidate with an SPDX SBOM, checksums, legal files and the pinned OpenAI tunnel-client v0.0.11. The App and helper slices, helper digest, package checksums and DMG integrity were inspected locally.
- The candidate remains a structural acceptance artifact only. Developer ID signing, notarization, staple, clean-machine Gatekeeper, real Runtime Key/Tunnel ID, isolated official Supervisor logins and ChatGPT Developer Mode acceptance remain external gates.
- Dynamic model semantics were rechecked: Luna is a recommendation only; `model/list` remains authoritative, another advertised model can be selected explicitly, and an unavailable selected model fails without substitution.
- The native task composer now advertises Luna as recommended only when the current catalog contains the exact Luna model; without it, the UI says to choose from the current catalog. Projection regression coverage confirms no Luna label or injected model appears when Luna is absent.
- Commit `ae598a0` adds a versioned local backup-package core. It is intentionally transport-only: callers must supply a consistent SQLite snapshot, and the package is not yet exposed in Settings or used for online database replacement.
- Commit (backup/restore loop) closes the P0 backup/restore item: Settings export/restore UI, three-file consistent SQLite online-backup snapshots, staged restore with schema/digest/data-root-inode validation, crash-safe swap and rollback, restart-time application before any store opens, and a quiescence gate that also blocks on tasks holding thread/worktree locks (including `unknown` tasks that survived recovery). Full evidence: 1960 Swift package tests, strict recursive `swift-format`, and a Debug arm64 App build all pass. `productionReviewAvailable` remains `false`; real Supervisor isolation, credentialed Tunnel/ChatGPT and signed release gates are unchanged.
