# Codex Bridge 双执行模式完整实现方案

> 日期：2026-08-18  
> 方案状态：完整目标方案，不是 MVP / 初步方案  
> 基线：当前 Codex Bridge V4 Service 架构 + CodexPro + DevSpace 已验证的 MCP 本地工作区实践  
> 核心目标：在不破坏现有 Codex 执行链路的前提下，让用户在 ChatGPT 对话中自行选择“交给 Codex”或“网页 ChatGPT 直接通过 MCP 修改本地项目并执行脚本”。默认始终使用 Codex，Direct Workspace 只在用户明确要求时启用。

---

# 1. 产品目标与用户实际体验

Codex Bridge 最终不再只有一种“ChatGPT → Codex → 本地项目”的执行方式，而是同时提供两条完整、互补的执行路径：

```text
ChatGPT Web
    │
    │ Secure MCP Tunnel
    ▼
Codex Bridge
    │
    ├────────────────────────────┐
    │                            │
    ▼                            ▼
Codex Agent Mode           ChatGPT Direct Mode
默认执行路径                用户显式选择
    │                            │
    ▼                            ▼
codex app-server          Direct Workspace Tools
    │                            │
    ▼                            ▼
本机 Codex 修改项目         GPT 直接修改项目
```

用户不需要在 macOS App 中切换“当前执行模式”，也不需要先调用一个专门的 `set_mode` 工具。**执行方式由用户在当前 ChatGPT 对话中的自然语言决定。**

默认行为固定为：

```text
用户：
“帮我修复这个 Bug”
“实现这个功能”
“修改一下项目”
“继续完成这个任务”

→ 默认走 Codex Agent Mode
→ ChatGPT 使用 submit_task / steer_task / get_task
→ 由本机 Codex 执行
```

只有用户明确表示希望 ChatGPT 自己操作本地文件时才进入 Direct Mode，例如：

```text
“你直接修改，不要调用 Codex”
“这次你自己通过 MCP 改”
“不要发给 Codex，你直接编辑本地文件”
“用插件直接完成”
“这次 Direct 做”
```

此时：

```text
→ 不调用 submit_task
→ ChatGPT 自己调用 direct_* MCP 工具
→ 读取本地文件
→ 修改 / 新建 / 删除 / 移动文件
→ 执行测试、构建、lint 或项目脚本
→ 读取命令输出
→ 根据结果继续修改
→ 完成后汇报真实改动和验证结果
```

模式选择属于**对话路由规则**，而不是安全边界。Codex Bridge 服务端不尝试理解自然语言，也不永久保存“这个用户喜欢 Direct”之类的状态。真正的安全边界始终由项目权限、路径安全、命令策略、本机审批和 Workspace 占用状态决定。

为了让 ChatGPT 稳定遵守默认行为，MCP 工具命名和 description 必须明确表达路由语义：

```text
submit_task
description:
“Default mutation/execution path. Use for implementation tasks unless the user
explicitly asks ChatGPT to modify the local workspace directly without Codex.”

direct_edit_project_file
description:
“Explicit Direct Workspace action. Use only when the user explicitly asks
ChatGPT itself to edit the local project without delegating the work to Codex.”
```

Direct 工具统一使用 `direct_` 前缀，不采用模糊的 `write`、`edit`、`bash` 名称。这样工具列表本身就能清楚区分：

```text
Agent Execution
submit_task
steer_task
interrupt_task

Direct Workspace
direct_write_project_file
direct_edit_project_file
direct_apply_project_patch
direct_manage_project_path
direct_exec_project_command
direct_read_command
direct_write_stdin
direct_interrupt_command
```

模式具有任务连续性：

```text
用户明确 Direct
→ 当前任务后续“继续”“再修一下”“跑一下测试”
→ 继续 Direct

当前任务是 Codex
→ “继续”“再处理一下”
→ 继续 Codex
```

如果用户明确说：

```text
“接下来都不要用 Codex，我自己让你直接改”
```

ChatGPT 可以依靠当前对话上下文保持 Direct 偏好；这一偏好仍然不进入 Codex Bridge SQLite，也不成为跨对话永久设置。

如果用户明确选择 Direct，但 Direct 权限不足、项目繁忙或操作被策略拒绝，**不得静默回退到 Codex**，因为那会违背用户明确选择。反过来也一样：默认或明确选择 Codex 时，不应因为 Codex 当前繁忙而偷偷改用 Direct。

---

# 2. 总体架构：保留 Codex，新增独立 Direct Workspace

当前正式架构继续保持：

```text
CodexBridge.app → BridgeServiceAppShell → BridgeIPC → CodexBridgeService
CodexBridgeService → BridgeServiceHost
BridgeServiceHost → BridgeServiceCore / BridgeServiceApplication / BridgeMCP
                  → BridgeCodexService / BridgeTunnel / BridgeSecurity
BridgeCodexService → BridgeCodexRPC
BridgeMCP → Service API
```

Direct Workspace 只在现有正式轻量架构中增加必要能力，不重新接回已经退出正式运行路径的旧 `BridgeCoordinator`、`BridgePipeline`、Evidence、Reporting 或旧 Verification 控制平面。

最终依赖结构建议为：

```text
BridgeMCP
   │
   ▼
BridgeServiceApplication
   │
   ├───────────────────────────┐
   │                           │
   ▼                           ▼
BridgeFiles               BridgeDirectCommand
   │                           │
   ▼                           ▼
BridgeSecurity             BridgeSecurity
                               │
                               ▼
                           Local Process

BridgeServiceApplication
   │
   └── ServiceWorkspaceMutationGate
              │
              ├── Direct Workspace
              └── Codex workspace-write Task
```

现有能力继续保留：

```text
bridge_status
list_projects
get_project
search_project_files
read_project_file
list_threads
read_thread
list_models
get_task
submit_task
steer_task
interrupt_task
```

在 Full MCP 模式中增加完整 Direct Workspace 工具：

```text
direct_write_project_file
direct_edit_project_file
direct_apply_project_patch
direct_manage_project_path

list_project_commands
direct_exec_project_command
direct_read_command
direct_write_stdin
direct_interrupt_command

get_project_changes
```

只读模式不暴露任何 mutation / process action，但仍可暴露：

```text
list_project_commands
get_project_changes
direct_read_command
```

其中 `direct_read_command` 只能读取已经存在且属于当前 MCP session / Service 的命令 session，不负责启动或改变进程。

当前 `MCPServiceExposureMode` 的：

```text
readOnly
full
```

保持不变，避免破坏现有配置和用户空间。App 只调整 Full 模式展示文案，例如：

```text
完整操作
Codex Agent + ChatGPT Direct Workspace
```

而不是再增加一个全局“Codex / Direct”选择器。

## Direct Workspace 与 Codex 的职责边界

两种方式都能修改代码，但定位不同：

```text
Codex Agent Mode
- 默认
- 独立 Agent
- 长时间自主执行
- Supervisor
- Codex Thread
- Codex 自己决定探索和修改步骤
- 复杂任务仍是首选

ChatGPT Direct Mode
- 用户显式选择
- 当前网页 GPT 自己执行
- MCP 工具就是 GPT 的本地 Coding Tools
- GPT 自己负责 read → edit → test → fix 循环
- 不创建 Codex Thread
- 不创建假 Codex Task
- 不启动 Supervisor
```

Direct 操作不能伪装成 Codex Task，否则会把两套执行语义重新搅在一起。Direct 的当前运行状态由 Service 内专门的 Direct operation / command session actor 管理，Codex Task 仍由 `ServiceTaskManager` 和现有 SQLite Task 状态负责。

---

# 3. Direct Workspace 完整能力设计

Direct Mode 的最终目标不是“只能改一行文本”，而是形成一个真正可用的本地 Coding Loop：

```text
search / read
→ 修改文件
→ 查看差异
→ 执行命令
→ 持续读取长命令输出
→ 根据错误继续修改
→ 再执行验证
→ 最终 review changes
```

参考 CodexPro 当前的 `read / write / edit / apply_patch / bash / show_changes`，以及 DevSpace Codex 模式的 `read / apply_patch / exec_command / write_stdin`，Codex Bridge 应实现同等级的基础工作能力，但继续使用当前更严格的项目注册、路径身份和权限体系。

## 文件读取增强

当前：

```text
read_project_file
```

保持兼容，同时新增：

```text
sha256
byte_count
file_revision
```

其中 `sha256` 必须针对完整文件，而不是当前分页内容。当前文件已经有严格大小上限，因此可以在 SecureFileReader 中一次完成完整文件 digest。

`file_revision` 可直接使用稳定格式：

```text
sha256:<hex>
```

或者内部使用结构：

```text
FileRevision {
  sha256
  byteCount
}
```

MCP 仍输出简单字段。

所有 Direct 修改都必须使用 revision precondition。这样即使：

```text
GPT 10:00 读取 A.swift
Codex 10:01 修改 A.swift
GPT 10:02 仍拿旧上下文尝试修改
```

Direct 写入也会失败为：

```text
file_revision_conflict
```

而不是把 Codex 的新代码覆盖掉。

## `direct_write_project_file`

用于：

```text
创建新文本文件
完整替换已有文本文件
```

建议输入：

```json
{
  "project_id": "prj-...",
  "relative_path": "Sources/NewFile.swift",
  "mode": "create",
  "content": "...",
  "expected_sha256": null,
  "create_parents": true,
  "client_request_id": "..."
}
```

已有文件完整替换：

```json
{
  "project_id": "prj-...",
  "relative_path": "Sources/App.swift",
  "mode": "replace",
  "content": "...",
  "expected_sha256": "..."
}
```

规则：

```text
create
→ 目标必须不存在
→ 不允许覆盖

replace
→ 目标必须存在
→ expected_sha256 必填
→ SHA 不一致拒绝

create_parents
→ 只允许在项目 root 内安全 mkdirat
→ 每一级都拒绝 symlink
```

输出：

```text
relative_path
operation
old_sha256?
new_sha256
byte_count
bounded_diff
```

## `direct_edit_project_file`

用于大多数日常代码修改。

输入：

```text
project_id
relative_path
expected_sha256
old_text
new_text
expected_replacements
```

默认：

```text
expected_replacements = 1
```

流程：

```text
读取当前完整文件
→ SHA 校验
→ old_text 精确查找
→ 匹配数量必须等于 expected_replacements
→ 生成新内容
→ 安全写盘
→ 返回 diff + new SHA
```

禁止“猜一个最接近位置然后修改”。

如果：

```text
0 match
2 matches 但 expected = 1
SHA 变化
```

全部失败，要求 ChatGPT 重新读取。

这与 CodexPro 当前推荐的 `read → expected_sha256 → write/edit` 模式一致。

## `direct_apply_project_patch`

提供 Codex 风格多文件 patch。

支持完整 coding 场景：

```text
Add File
Update File
```

删除和移动不混进普通 patch，而交给独立 destructive tool，这样 `direct_apply_project_patch` 可以正确标记：

```text
destructiveHint = false
```

Patch 流程：

```text
完整解析 patch
→ 拒绝绝对路径
→ 拒绝 ..
→ 敏感路径策略
→ 项目 root identity
→ 每个 existing file revision 校验
→ 所有 hunk 在内存 staging
→ 全部 semantic validation 完成
→ 逐文件安全 commit
→ 返回该 patch 自己的 bounded diff
```

多文件 patch 在逻辑错误出现前不能修改任何文件。真正 commit 时每个文件使用原子单文件写入；如果发生罕见 OS I/O failure，服务尝试用本次内存中的原内容回滚已提交文件，并诚实返回：

```text
partial_commit
changed_files
rollback_status
```

不为了获得“跨多个普通文件的绝对 crash transaction”重新引入持久 Saga / Ledger。Service 崩溃属于单独的故障域，恢复后通过实际 Git diff 和 revision 判断真实状态。

## `direct_manage_project_path`

专门承载 destructive 文件系统动作：

```text
delete_file
move_file
create_directory
delete_empty_directory
```

工具本身：

```text
readOnlyHint = false
destructiveHint = true
```

删除文件必须携带：

```text
expected_sha256
```

移动文件必须携带：

```text
source_expected_sha256
destination_expected_absent = true
```

不能 blind delete，也不能 blind overwrite destination。

默认不允许递归删除目录。代码项目需要清理目录时，只允许：

```text
删除已空目录
```

需要大规模删除时仍建议走 Codex，并由现有 Codex 审批链处理。

## SecureProjectFileWriter

新增：

```text
Packages/BridgeCore/Sources/BridgeSecurity/
  SecureProjectFileWriter.swift
```

继续沿用当前 `SecureFileReader` 的 descriptor-relative 风格，而不是普通 Foundation path write。

安全流程：

```text
打开 RegisteredRoot directory fd
→ fstat root，验证 device / inode
→ SecureRelativePath
→ SensitivePathPolicy
→ ProjectFilePolicy
→ openat / mkdirat
→ O_NOFOLLOW
→ 验证父目录
→ 验证目标类型
→ SHA precondition
→ 同目录 staging file
→ bounded write
→ fsync staging
→ 最终再次校验目标 identity / revision
→ atomic renameat
→ fsync parent directory
```

仅支持普通 UTF-8 文本源码和配置文件。Binary mutation 不属于这套 Direct coding surface。

对于已有文件：

```text
st_nlink == 1
→ atomic replacement

st_nlink > 1
→ Direct 拒绝 unsupported_hard_link
```

这样不会为了“保留 hard link”退回可能在 Service 崩溃后留下半文件的 truncate-in-place 写法。罕见 hard-link 文件可以由用户改用 Codex 或本机编辑器处理。

文件仍必须拒绝：

```text
.env*
私钥
浏览器数据
Codex auth
Runtime Key
本地 MCP secret
.git 内部文件
项目 root 外路径
symlink / alias escape
device node
socket
FIFO
binary
超限文件
```

## Change Review

增加只读：

```text
get_project_changes
```

它不是第二个 Evidence Store，只是受限运行 Git inspection：

```text
git status --porcelain=v1
git diff --no-ext-diff
git diff --cached --no-ext-diff
```

要求项目确实是 Git repository；否则返回：

```text
not_git_repository
```

输出包括：

```text
changed_files
bounded_diff
additions
deletions
truncated
```

Direct 文件工具自身也会返回本次操作的局部 diff。`get_project_changes` 用于完成一个较长 Direct 任务后的整体 review。

---

## Direct 命令与脚本执行

用户要求网页 GPT 能“运行一些测试、脚本”，因此最终功能必须包含真正的 Process Session，而不是只提供一个固定 `run_tests`。

新增正式模块：

```text
BridgeDirectCommand
```

职责只有：

```text
DirectCommandPolicy
DirectCommandRunner
DirectCommandSessionManager
BoundedCommandOutput
DirectProcessLifetime
```

不要让它依赖旧：

```text
BridgePipeline
BridgeCoordinator
旧 Verification orchestration
Evidence Store
```

旧 `BridgeVerification/BoundedVerificationProcess.swift`、`CommandPolicy.swift` 中已经验证有效的：

```text
process group
timeout
stdout/stderr bound
working directory fd identity
cancel
argv policy
```

可以提取、重构为正式轻量 primitive，但不要恢复旧控制平面。

### 项目命令策略

项目增加：

```text
direct_command_mode
```

取值：

```text
denied
registered
safe
```

默认：

```text
registered
```

而不是 unrestricted shell。

`registered`：

用户在 macOS App 本地明确登记项目命令，例如：

```text
Codex Bridge Tests
Scripts/with-xcode.sh swift test --package-path Packages/BridgeCore

MCP Inspector
Scripts/verify-mcp-inspector.sh

Frontend Tests
npm run test
```

每条命令存储稳定 command ID：

```text
wcmd_<sha256 canonical spec>
```

ChatGPT 可以：

```text
list_project_commands
→ direct_exec_project_command(command_id=...)
```

`safe`：

除 registered command 外，允许受程序化 Policy 判断的常见开发命令，例如：

```text
git status
git diff
git log
git show
git rev-parse

swift test
swift build

npm run test
npm run build
npm run lint
npm run typecheck

pnpm / yarn 对应安全 scripts

pytest
go test
cargo test
cargo check
cargo clippy
tsc
eslint
biome check
```

Dynamic command 协议使用结构化 argv：

```json
{
  "project_id": "prj-...",
  "argv": [
    "Scripts/with-xcode.sh",
    "swift",
    "test",
    "--package-path",
    "Packages/BridgeCore"
  ],
  "working_directory": ".",
  "tty": false,
  "yield_time_ms": 1000,
  "timeout_ms": 300000
}
```

核心协议不接受：

```text
"command": "foo && bar > x"
```

这样直接消除大部分 shell separator / redirection / command substitution 注入面。

Dynamic safe mode 明确拒绝：

```text
sudo / doas
rm
diskutil
launchctl
installer
security
pbpaste
ssh / scp / sftp
curl / wget
git push
git reset --hard
git clean
npm publish / package publish
docker
任意 shell -c
heredoc
>
>>
|
;
&&
||
$()
backtick
绝对 project target
../
敏感路径参数
```

如果项目确实需要复杂 Shell 脚本，应把脚本文件放在已批准项目内，然后在 App 中登记成 `registered command`。这样用户明确知道“这个项目允许 GPT Direct 执行什么”，而不是把一个万能 shell 交给远程模型。

网络仍独立受：

```text
project.accessPolicy.network
```

控制。registered command 可以声明：

```text
requiresNetwork = true
```

若项目 network denied，则拒绝启动。

### Process Session

`direct_exec_project_command` 启动后等待一个短 yield window：

```text
快速完成
→ 直接返回 exit code + output

仍在运行
→ 返回 command_session_id + running
```

长命令由：

```text
direct_read_command
```

分页/增量读取输出。

必须同时实现：

```text
direct_write_stdin
```

用于真正需要 stdin / PTY 的本地脚本；只有 `tty=true` session 才允许输入。

以及：

```text
direct_interrupt_command
```

用于：

```text
SIGTERM process group
→ bounded grace period
→ SIGKILL
→ reap
→ 释放 Workspace lease
```

Output Buffer 参考 DevSpace 的 bounded head/tail 思路：

```text
保留前部关键输出
+
保留末尾错误输出
+
中间超限部分标记 omitted
```

不能把几十 MB 构建日志回传给 ChatGPT。

每个 command session 必须有：

```text
最大运行时间
最大 stdout
最大 stderr
最大 session 数
最大 stdin payload
idle TTL
completed retention TTL
```

所有 session 都属于后台 `CodexBridgeService`，App 关闭不能使它中止；Service 自身停止或崩溃时必须保证 command process tree 被清理。

macOS 没有 Linux `PR_SET_PDEATHSIG`，因此不能只依赖 Swift `deinit`。完整实现应提供一个轻量 parent-liveness mechanism：Service 为 Direct command 建立 process group 和生命周期 pipe，并由受控 reaper/watchdog 保证 Service fd 消失后终止对应 process group。正常路径仍由 `DirectCommandSessionManager` 主动 reap。

---

# 4. Codex 与 Direct 的统一并发、切换与协作

这是双模式能否真正共存的核心。

当前数据库已有：

```text
bridge_service_one_active_write_task
```

它已经防止：

```text
Codex write task A
vs
Codex write task B
```

但无法防止：

```text
Codex
vs
GPT Direct
```

因此新增：

```text
ServiceWorkspaceMutationGate
```

它只解决“谁当前可以稳定操作这个 project workspace”，不是新的 Task Store，也不新增第二数据库。

## Workspace 占用模型

同一个真实 project checkout 同一时间只能存在一个需要稳定 workspace 的 active owner。

Owner：

```text
codexWriteTask(taskID)
directFileMutation(operationID)
directCommand(commandSessionID)
```

普通：

```text
read_project_file
search_project_files
get_project_changes
get_task
read_thread
```

仍然是 observation，不获取长期 writer slot。

Direct test / build 即使理论上只“验证代码”，也必须在执行期间占用稳定 workspace slot，因为测试和构建需要针对一个一致源码快照运行，而且它们通常会写 `.build`、缓存或生成物。

因此：

```text
Codex 正在改
→ 不允许 Direct test

Direct test 正在跑
→ 不允许 Codex 开始 workspace-write

Direct edit 正在进行
→ 不允许第二个 Direct mutation
```

选择保守的“每项目一个 active workspace owner”，而不是一开始允许两个 Direct session 同时改不同文件。这样和当前 Codex Bridge 的项目级单写者原则一致，状态也最清晰。

## Gate 不能只“先查一下数据库”

如果：

```text
Direct：
查 DB，没有 Codex Task

同时 Codex：
也查状态，然后插入 write Task
```

会产生 TOCTOU。

正确流程：

```text
ServiceWorkspaceMutationGate actor
```

先建立短时 admission reservation。

Direct：

```text
gate.reserve(project, directOperation)
→ reservation 成功
→ 查询 DB 是否已经有 active workspace-write task
→ 若有：释放 reservation，返回 project_busy
→ 若无：reservation 升级为 Direct lease
→ 执行
→ 释放
```

Codex：

```text
gate.reserve(project, codexAdmission)
→ 若 Direct lease 存在：project_busy
→ 调用 tasks.submit(workspace-write)
→ DB partial unique index 创建长期 Codex 约束
→ 释放 codexAdmission reservation
```

Codex Task 运行期间不需要 Gate 再复制一份长期 task state。Direct 每次 admission 都通过 `ServiceTaskManager` 查询 active workspace-write Task。SQLite 仍然是 Codex Task 状态的 Single Source of Truth。

这样：

```text
Gate
→ Direct vs Direct
→ Direct vs Codex admission

SQLite unique index
→ Codex vs Codex
```

不会出现两套长期状态。

必须审计所有生产 `tasks.submit` 调用。目前正式路径集中在 `BridgeServiceApplication.serviceSubmitTask()`；实现时应继续确保 App/MCP 都通过统一应用服务入口，禁止增加绕过 Gate 的新写任务入口。

## 文件 revision 是第二道保护

Gate 只协调 Codex Bridge 自己控制的 writer。

它无法锁住：

```text
VS Code
Xcode
用户 Terminal
其他本地 Agent
其他进程
```

因此 Direct 文件操作仍必须使用 SHA precondition。

完整保护是：

```text
内部 writer serialization
+
文件级 revision compare
+
descriptor identity validation
```

对于 Codex Bridge 外部的未协调本地写进程，无法提供理论上的跨进程 content-CAS；在最终 atomic rename 前执行一次 descriptor / identity / SHA 复核，最大限度缩小 race window。如果复核发现变化立即拒绝。

## 模式切换期间的规则

如果 Codex workspace-write 正在运行，用户说：

```text
“别让 Codex 做了，我现在要你直接改”
```

ChatGPT 首次 Direct mutation 会得到：

```json
{
  "error": "project_busy",
  "owner": "codex_task",
  "task_id": "task-..."
}
```

**不能自动 interrupt Codex。**

ChatGPT 应告诉用户当前 Codex 仍占用项目。如果用户明确要求停止，再调用：

```text
interrupt_task
```

Codex 进入 terminal 后 Direct 才能继续。

反过来：

```text
Direct command 正在运行
→ 用户要求改回 Codex
```

`submit_task(workspace-write)` 返回：

```text
project_busy
owner = direct_command
command_session_id = ...
```

也不能自动杀掉 Direct process。只有用户明确要求取消时调用：

```text
direct_interrupt_command
```

这个原则保证“模式切换”不会变成隐式破坏行为。

## 两种模式如何协作

完整目标不是同时抢同一个 checkout，而是安全串行协作：

```text
GPT 分析
→ Codex 完成大型实现
→ Codex Task terminal
→ GPT 读取最新文件
→ 用户要求 Direct
→ GPT 做小修
→ Direct test
```

或者：

```text
GPT Direct 做初始修复
→ test 发现问题较复杂
→ 用户决定交给 Codex
→ Direct session 完成并释放
→ submit_task
```

当前版本**明确不实现同一 checkout 上 Codex 与 Direct 的同时写入**。这不是缺失功能，而是设计约束。未来如果产品真的需要两个 Agent 同时开发，应使用不同 Git worktree 做 workspace isolation，而不是放松这个 Gate。

---

# 5. 权限、安全审批与 MCP 边界

Direct Mode 是用户显式选择，但“用户在聊天里说直接改”不等于绕过本机权限。

所有操作继续服从当前项目：

```text
read
write
network
```

权限。

## 文件操作

```text
write = denied
→ Direct mutation 拒绝

write = allowed
→ 通过其他安全检查后直接执行

write = requiresLocalApproval
→ 创建 Direct approval request
```

为了实现完整能力，这次方案应正式支持 Direct 本机审批，而不是要求用户先把 write 改成 allowed。

但 Direct approval 不应变成第二套 Task Pipeline。

新增轻量：

```text
DirectActionApprovalCenter
```

位于后台 Service，状态只存在内存，Service 重启自动失效。

Pending approval 只保存：

```text
approval_id
project_id
action_kind
safe_summary
payload_digest
created_at
expires_at
```

不保存：

```text
完整敏感文件内容
Token
绝对路径
Shell secret
```

当 Direct 工具触发本机审批时：

```text
MCP request
→ Service 规范化请求
→ SHA256(normalized action payload)
→ 创建 approval
→ 返回 approval_required + approval_id
→ macOS App 显示动作摘要
```

App 只能：

```text
Approve once
Deny
```

批准绑定 exact payload digest。

批准后 ChatGPT 重新提交同一个：

```text
client_request_id
```

Service 只有在：

```text
project
action
paths
payload digest
```

完全一致时消费授权一次。

如果 ChatGPT 改了任意参数：

```text
授权失效
重新审批
```

Service 重启后 pending / approved-but-unconsumed action 全部安全失效。

这不是长期 Ledger，只是一次性 capability handoff。

## Direct command 权限

命令执行还必须通过：

```text
direct_command_mode
```

即使：

```text
write = allowed
```

也不能自动获得任意进程执行权限。

建议：

```text
direct_command_mode = denied
→ 不允许启动任何 Direct command

registered
→ 只允许 App 明确登记命令

safe
→ registered + 内置安全开发命令
```

如果命令自身会产生项目文件修改，仍需要：

```text
write != denied
```

如果需要网络：

```text
network != denied
```

如果对应 permission 为 `requiresLocalApproval`，进入同一个 Direct approval flow。

高风险操作不进入 safe dynamic command。例如 package install、git mutation、外部网络上传等，如果确实要 Direct 执行，必须作为本机 registered command，并且 App 将其显示为高风险命令，建议始终 `Approve once`，不能因为项目 write=allowed 就无提示执行。

## MCP annotation 必须真实

查询工具：

```text
readOnlyHint = true
destructiveHint = false
```

普通 write/edit/patch：

```text
readOnlyHint = false
destructiveHint = false
```

delete/move：

```text
readOnlyHint = false
destructiveHint = true
```

command launch：

```text
readOnlyHint = false
destructiveHint = false
```

interrupt：

```text
readOnlyHint = false
destructiveHint = true
```

不能为了让 ChatGPT 更容易调用而把 Direct mutation 伪装成 read-only。

CodexPro 和 DevSpace 已经证明自定义 MCP 可以给 ChatGPT 暴露本地 write/edit/patch/process primitives，但 Host 本身仍可能根据具体动作额外执行 Safety 检查；Codex Bridge 应接受这一层平台约束，而不是用错误 metadata 规避。

---

# 6. 数据模型、模块、App 与协议改造

当前 Service schema 为：

```text
V4
```

项目表已有：

```text
project_id
name
canonical_path
root_device
root_inode
read_permission
write_permission
network_permission
...
```

完整 Direct Workspace 需要一次明确的 schema migration，而不是把命令配置放到散乱 settings key。

## Schema V5

建议 `bridge_service_projects` 增加：

```text
direct_command_mode TEXT NOT NULL DEFAULT 'registered'
workspace_commands_json BLOB NOT NULL DEFAULT '[]'
```

`direct_command_mode`：

```text
denied
registered
safe
```

`workspace_commands_json` 为有界数组。

模型：

```text
ServiceWorkspaceCommand {
    id
    name
    executable
    arguments
    workingDirectory
    requiresNetwork
    risk
}
```

`risk`：

```text
normal
elevated
```

其中 elevated registered command 每次仍需要 Direct local approval。

旧项目迁移：

```text
V4 → V5
direct_command_mode = registered
workspace_commands = []
```

所以升级不会突然向 ChatGPT 开放任何新的可执行命令。

不新增：

```text
第二个数据库
workspace ledger
direct task table
approval history table
```

Command Session 和 Direct Approval 是 Service runtime state；Service 崩溃后全部终止/失效，而不是伪恢复。

## `ServiceProjectRecord`

增加：

```text
directCommandMode
workspaceCommands
```

`get_project` 不再把 command/verification 信息写死为空，而返回受限的：

```text
direct_workspace:
  file_write_permission
  command_mode
  commands[]
```

不返回绝对项目路径。

## BridgeSecurity

增加：

```text
SecureProjectFileWriter
SecureProjectDirectoryMutation
```

复用：

```text
RegisteredRoot
SecureRelativePath
SensitivePathPolicy
ProjectPathResolver 的边界原则
```

对写路径使用 descriptor-relative implementation，不允许“先 Foundation resolve，再普通 path write”。

## BridgeFiles

保持读取职责清晰：

```text
RestrictedProjectFileService
  read
  search
```

新增：

```text
RestrictedProjectMutationService
  write
  edit
  applyPatch
  managePath
```

不把 read/search/mutation 全塞回一个千行文件。

## BridgeDirectCommand

新增正式 target：

```text
BridgeDirectCommand
```

内部拆分：

```text
DirectCommandPolicy.swift
DirectCommandRunner.swift
DirectCommandSessionManager.swift
DirectCommandOutputBuffer.swift
DirectProcessLifetime.swift
```

`BridgeServiceApplication` 增加对该模块依赖。

## BridgeServiceApplication

增加：

```text
ServiceWorkspaceMutationGate
DirectActionApprovalCenter
```

以及新的 Service API：

```text
serviceDirectWriteFile
serviceDirectEditFile
serviceDirectApplyPatch
serviceDirectManagePath

serviceProjectCommands

serviceDirectExecCommand
serviceDirectReadCommand
serviceDirectWriteStdin
serviceDirectInterruptCommand

serviceProjectChanges
```

所有 MCP action 只调用 Service API。`BridgeMCP` 永远不能直接访问 FileManager 或 Process。

`serviceSubmitTask` 的 workspace-write admission 也必须进入 `ServiceWorkspaceMutationGate`。

## BridgeMCP

扩展：

```text
MCPServiceToolName
MCPServiceToolCatalog
MCPServiceToolDispatcher
BridgeMCPServiceAPI
DTO
schemas
result encoder
error mapping
```

统一 public error，例如：

```text
project_busy
file_revision_conflict
path_forbidden
path_changed
write_not_allowed
command_not_allowed
network_not_allowed
approval_required
approval_expired
invalid_patch
command_session_not_found
command_timeout
output_limit_exceeded
```

不得把底层：

```text
errno
绝对路径
Swift Error 描述
进程环境
```

直接透传到 ChatGPT。

## BridgeIPC

XPC 增加：

```text
读取 / 更新 project direct command mode
读取 / 保存 registered commands
列出 pending Direct approvals
批准 / 拒绝 Direct action
列出 active Direct command sessions
取消 Direct command
```

所有 DTO 继续版本化、有硬上限。

## BridgeServiceAppShell

项目页保持“本机配置、安全控制”定位。

在现有：

```text
读取权限
写入权限
网络权限
```

下面增加：

```text
ChatGPT Direct 命令
  禁止
  仅登记命令
  安全开发命令
```

增加“项目命令”编辑区：

```text
名称
Executable / Project Script
Arguments
Working Directory
是否需要网络
风险等级
```

App 需要明显说明：

```text
默认仍由 Codex 执行项目任务。
这里配置的是用户在 ChatGPT 对话中明确要求“直接执行”时，
网页 GPT 可以使用的本地能力。
```

Connections 页 Full MCP 描述调整为：

```text
完整模式会暴露：
Codex 任务提交/纠偏/中断
+
ChatGPT Direct Workspace 文件编辑与受控命令执行
```

App 增加当前 workspace owner 展示：

```text
空闲
Codex 正在写入 · task xxx
ChatGPT Direct 正在编辑
ChatGPT Direct 正在运行 tests · session xxx
```

以及 Pending Direct Approval 卡片。

---

# 7. 完整实施顺序

下面的阶段只是为了让代码始终可构建、可回退，**不是把功能拆成“首版”和“以后再说”**。最终只有全部阶段完成并通过真实 ChatGPT 验收，才算本方案完成。

## 阶段 A：基础并发与 revision

先完成：

```text
ServiceWorkspaceMutationGate
active Codex write query
read_project_file SHA-256 / byte_count
统一 project_busy public error
```

让现有 Codex `workspace-write` Task 先进入新 Gate admission，但不改变正常 Codex 用户体验。

必须先证明：

```text
Codex vs Codex 仍受 DB unique index 保护
Direct reservation vs Codex admission 不可能同时通过
不同 project 可以并行
read-only operation 不受影响
unknown Codex write task 继续占用
```

## 阶段 B：完整 Direct 文件系统

完成：

```text
SecureProjectFileWriter
RestrictedProjectMutationService

direct_write_project_file
direct_edit_project_file
direct_apply_project_patch
direct_manage_project_path
get_project_changes
```

同时完成：

```text
SHA stale protection
atomic single-file commit
path identity revalidation
bounded diff
create parents
delete/move revision precondition
hard-link reject
敏感文件完整覆盖测试
```

这一阶段完成后，ChatGPT 应已经能在真实网页会话中：

```text
明确说“你直接改，不调用 Codex”
→ read
→ edit
→ create
→ patch
→ delete/move
→ review diff
```

## 阶段 C：Project Command 数据与 App 配置

完成 Schema V5：

```text
direct_command_mode
workspace_commands_json
```

完成：

```text
ServiceWorkspaceCommand
稳定 command ID
App command editor
IPC configuration
list_project_commands
```

迁移必须保证旧 V4 数据原样保留、项目权限不变化。

## 阶段 D：完整 Direct Process Session

完成：

```text
DirectCommandPolicy
DirectCommandRunner
DirectCommandSessionManager
BoundedCommandOutput
DirectProcessLifetime

direct_exec_project_command
direct_read_command
direct_write_stdin
direct_interrupt_command
```

同时完成：

```text
registered mode
safe mode
structured argv
project working-directory restriction
network policy
timeout
process group
PTY
stdin
output head/tail
session TTL
cancel
Service shutdown cleanup
Service crash orphan cleanup
Workspace Gate lifetime
```

不是只做一次性 `run_tests`。

## 阶段 E：完整 Direct 本机审批

完成：

```text
DirectActionApprovalCenter
XPC pending approvals
App Approve once / Deny
payload digest binding
client_request_id replay
expiry
Service restart invalidation
```

覆盖：

```text
file write requiresLocalApproval
destructive path action
command permission
network approval
elevated registered command
```

ChatGPT 不具备批准能力。

## 阶段 F：模式路由与真实 MCP 工具语义

完善所有 `Tool.description`，明确：

```text
Codex = default
Direct = explicit opt-in only
```

增加 Full/ReadOnly 暴露测试和 MCP Inspector fixture。

真实 ChatGPT 对话至少测试这些表达：

```text
“帮我修改”
→ Codex

“直接改，不要调用 Codex”
→ Direct

“这次用 Codex”
→ Codex

Direct 任务中“继续”
→ Direct

Codex 任务中“继续”
→ Codex
```

这里验证的是 ChatGPT 实际工具选择，不只验证 MCP Server tool catalog。

## 阶段 G：双模式协作、故障与产品收尾

完成所有交叉场景：

```text
Codex active → Direct busy
Direct edit → Codex busy
Direct long command → Codex busy
Codex terminal → Direct 可以进入
Direct terminal → Codex 可以进入
stale SHA → Direct 不覆盖
用户本地编辑 → stale revision conflict
Tunnel 断开 → 已运行本地命令行为正确
App 退出 → Service / Direct session 继续
Service crash → Direct process 不成为孤儿
```

更新：

```text
AGENTS.md
正式架构文档
MCP Inspector
App 帮助文案
安全说明
真实 ChatGPT 验收记录
```

`AGENTS.md` 只记录最终长期架构事实，不记录开发过程台账，也不随功能 commit 提交，除非用户明确要求。

每个阶段形成一个可构建、可测试、可回退的独立 Git commit。不得提交半完成的跨阶段状态。

---

# 8. 完整测试矩阵与最终验收

测试必须验证真实文件、SQLite、真实子进程和真实并发，不以“Mock 方法调用过”代替。

## 文件与路径安全

至少覆盖：

```text
正常 create
正常 replace
正常 exact edit
正常 multi-file patch
正常 move
正常 delete
missing parent create
expected SHA mismatch
expected absent 但文件已存在
目标在读取后被替换
父目录被替换
root device/inode 改变
absolute path
../
symlink file
symlink parent
hard link
.env
.env.local
private key
Codex auth
browser profile
.git/config
.git/index
binary
FIFO
socket
oversized file
oversized write
oversized diff
UTF-8 invalid
```

每个失败场景都必须证明：

```text
目标文件没有被半修改
没有项目外副作用
返回 public bounded error
```

## 并发

必须使用真正并发调用验证：

```text
Direct edit vs Direct edit
Direct patch vs Direct edit
Direct command vs Direct edit
Direct command vs Direct command
Direct vs Codex write admission
Codex write admission vs Direct
Codex write task vs Direct
different project parallel
read-only operations parallel
```

重点 stale case：

```text
read A → SHA 1
外部/ Codex 修改 A → SHA 2
Direct expected SHA 1
→ conflict
→ SHA 2 内容保持不变
```

## 命令执行

真实 process tests：

```text
正常 exit 0
exit non-zero
stdout
stderr
大量 stdout
大量 stderr
head/tail truncation
timeout
SIGTERM
SIGKILL fallback
process child tree
interactive stdin
PTY
Ctrl-C
session polling
completed session retention
session TTL
Service normal shutdown
Service forced crash
parent-liveness cleanup
cwd root identity changed
forbidden argv
shell injection
network denied
registered normal command
registered elevated command
safe dynamic command
unsafe dynamic command
```

## 审批

验证：

```text
requiresLocalApproval 创建 pending
Approve same digest 成功
Deny
过期
重复消费
不同 payload 不能复用 approval
不同 project 不能复用
Service restart 后 approval 无效
ChatGPT 不能通过 MCP 自行 approve
```

## 数据库迁移

V4 → V5：

```text
原项目 ID 不变
root identity 不变
read/write/network 不变
Task / messages 不变
commands 默认 []
direct mode 默认 registered
重复启动幂等
迁移中断回滚
未知 schema 拒绝
```

## MCP 契约

验证：

```text
ReadOnly 不暴露 mutation
Full 暴露 Codex + Direct

Direct 工具命名明确
Annotations 正确

unknown args 拒绝
oversized args 拒绝
deadline
result bound
secret redaction
busy owner response
approval response
revision conflict response
command session response
```

## Never Break Userspace

现有功能必须全部保持：

```text
list/search/read project
Codex Threads
Codex models
submit_task
steer_task
interrupt_task
Task conversation streaming
reasoning / tool-call streaming
Supervisor
Tunnel
App 退出后 Service 继续
legacy import
```

所有现有测试继续通过。

## 最终真实 ChatGPT Developer Mode 验收

最终验收不是 MCP Inspector 能看见工具，而是以下真实对话全部通过。

### 默认 Codex

用户：

```text
“帮我修复这个测试失败的问题”
```

预期：

```text
ChatGPT 不调用 direct mutation
→ submit_task
→ Codex 修改
→ Task 正常完成
```

### 用户显式 Direct

用户：

```text
“这次你直接修改，不要调用 Codex，修好以后运行测试。”
```

预期：

```text
ChatGPT
→ search/read
→ direct_edit / patch
→ direct_exec_project_command
→ direct_read_command（如需要）
→ 根据测试结果继续 direct edit
→ tests passed
→ get_project_changes
→ 汇报
```

整个过程中：

```text
没有 submit_task
没有 Codex Thread
没有 Supervisor
```

### Direct 审批

项目：

```text
write = requiresLocalApproval
```

用户要求 Direct。

预期：

```text
Direct action → approval_required
→ App 显示精确摘要
→ 用户批准
→ 同一 payload 重试
→ 成功
```

### Codex 与 Direct 冲突

Codex workspace-write 正在运行。

用户：

```text
“你现在直接改这个文件，不要用 Codex。”
```

预期：

```text
Direct → project_busy
→ 文件不变
→ ChatGPT 不自动 interrupt Codex
```

### stale revision

GPT Direct 已读文件，用户在 Xcode 修改并保存。

GPT 再 edit。

预期：

```text
file_revision_conflict
→ 不覆盖用户新内容
→ ChatGPT 重新读取
```

### Direct 长测试

用户：

```text
“直接修改然后跑完整测试。”
```

测试超过 yield window。

预期：

```text
direct_exec → session
→ direct_read_command
→ 持续 bounded 输出
→ completion
→ lease 释放
```

### App 关闭

Direct command 正在执行时关闭 macOS App。

预期：

```text
后台 Service 与 command 继续
```

### Service 崩溃

强制终止 Service。

预期：

```text
Direct command process group 被 reaper 清理
→ 不留下孤儿 process
→ 重启后没有伪造 running session
```

只有上述全部通过，才视为“ChatGPT Direct Workspace + Codex 双执行模式”完整实现。

---

# 最终架构结论

最终 Codex Bridge 的产品模型应稳定为：

```text
                        ChatGPT Web
                            │
                    Secure MCP Tunnel
                            │
                            ▼
                     Codex Bridge MCP
                            │
              ┌─────────────┴─────────────┐
              │                           │
              ▼                           ▼
       Default: Codex              Explicit: Direct
        Agent Execution              Workspace
              │                           │
              ▼                           ├── File Read/Search
       submit / steer                     ├── Write/Edit/Patch
       interrupt / task                   ├── Move/Delete
              │                           ├── Change Review
              ▼                           └── Process Sessions
       Codex app-server                         │
              │                                 ▼
              ▼                          Local Project/Process
         Local Project                           │
              │                                  │
              └─────────────┬────────────────────┘
                            ▼
              ServiceWorkspaceMutationGate
```

整个设计只坚持三个长期原则：

```text
1. 用户没有明确选择 Direct 时，修改/实现任务默认交给 Codex。

2. 用户明确选择 Direct 后，网页 ChatGPT 可以完整完成
   read → modify → run → inspect → fix 的本地 Coding Loop，
   但仍服从项目权限和本机安全边界。

3. 同一个真实 checkout 同一时间只有一个 active workspace owner，
   再辅以 SHA revision，防止 Codex、GPT Direct 和旧上下文互相覆盖。
```

这使两种执行方式不是互相替代，而是成为同一个 Codex Bridge 下两种由用户在对话中自行选择的执行器：复杂任务默认交给 Codex；用户想让当前网页 GPT 亲自处理时，可以明确切到 Direct，任务完成后又可以随时回到 Codex。

---

# 参考实现与核对依据

本方案在 2026-08-18 核对了以下当前项目与官方仓库内容。

## 当前 Codex Bridge

当前正式架构和长期规则：

```text
AGENTS.md
Packages/BridgeCore/Sources/BridgeMCP/MCPServiceToolCatalog.swift
Packages/BridgeCore/Sources/BridgeServiceApplication/
Packages/BridgeCore/Sources/BridgeServiceCore/ServiceStoreSchema.swift
Packages/BridgeCore/Sources/BridgeFiles/
Packages/BridgeCore/Sources/BridgeSecurity/
```

当前确认事实：

```text
Service schema = V4
MCP exposure = readOnly / full
Full 当前只有 submit_task / steer_task / interrupt_task 等 Agent action
Project 已存在 read / write / network 权限
SQLite 已存在 one-active-workspace-write Codex Task 约束
旧 Verification / Pipeline 不属于正式产品运行路径
```

## CodexPro

Repository:

https://github.com/rebel0789/codexpro

README:

https://github.com/rebel0789/codexpro/blob/main/README.md

Changelog:

https://github.com/rebel0789/codexpro/blob/main/CHANGELOG.md

FAQ:

https://github.com/rebel0789/codexpro/blob/main/FAQ.md

重点参考：

```text
write / edit / guarded apply_patch
safe bash
show_changes
expected_sha256
canonical per-file write serialization
workspace-scoped path safety
```

## DevSpace

Repository:

https://github.com/Waishnav/devspace

ChatGPT Coding Workflow:

https://github.com/Waishnav/devspace/blob/main/docs/chatgpt-coding-workflow.md

Configuration:

https://github.com/Waishnav/devspace/blob/main/docs/configuration.md

Security:

https://github.com/Waishnav/devspace/security

重点参考：

```text
ChatGPT 通过 MCP 直接操作本地 workspace
Codex tool mode
apply_patch
exec_command
write_stdin
long-running process session
bounded process output
worktree isolation 思路
```

本方案只吸收这些项目已经证明有效的 coding primitives，不复制其 Node/TypeScript runtime，也不改变 Codex Bridge 当前 Swift 6、LaunchAgent、XPC、Secure Tunnel、单 SQLite 和本机安全模型。
